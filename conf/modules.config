process {
    // Process-specific configurations. Output publishing is handled natively in main.nf via the output block.
    withName: '.*MINIMAP2_ALIGN.*' {
        ext.args = { params.technology == 'ONT' ? '-ax map-ont' : (params.technology == 'PB' ? '-ax map-pb' : '-ax map-hifi') }
    }
}
